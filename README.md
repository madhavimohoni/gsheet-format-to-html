# gsheet-format-to-html
Get HTML tags and text from a formatted Google Sheet range (cell) using Google Apps Script using RichText and runs. Can also paste this code in your editor and use as custom formula on the Google Sheet itself.
